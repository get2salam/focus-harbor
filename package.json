{
  "name": "focus-harbor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first deep work session workspace.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "verify": "npm test && node --check js/main.js && node scripts/verify-static.mjs"
  }
}
