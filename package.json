{
  "name": "focus-harbor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first deep work session workspace.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/main.js js/scoring.js scripts/preview-score.mjs scripts/verify-static.mjs",
    "example:score": "node scripts/preview-score.mjs",
    "verify": "npm test && npm run check && node scripts/verify-static.mjs && npm run example:score"
  }
}
